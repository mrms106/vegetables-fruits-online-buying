<%-include("../layouts/head.ejs") %>

	<div class="container">
		<h1 class="detail_title"><b>Add Details</b></h1>
		<form class="order-form" method="post" action="/home/confirm">
			<label class="lab">Enter Your  Name</label>
			<input class="textfield" type="text" name="buy[name]" placeholder="Last Name"><br>
			<label class="lab">Enter Your Address</label>
			<input class="textfield tf1" type="text" name="buy[address]" placeholder="Address"><br>
			<label class="lab">Enter Your Area Pincode</label>
			<input class="textfield tf4" type="number" name="buy[pincode]" placeholder="Pincode"><br>
			
				<label class="lab"><%=Veg.title%></label>
				<input type="hidden" name="buy[product]" value="<%=Veg.title%>">
				<input class="textfield" type="number" id="price" name="buy[quantity]" value="1"  required ><br>
			   <input type="hidden" name="price" id="basePrice" value="<%=Veg.price%>">
				<label class="lab">Total Price</label>
				<input id="totalPriceInput" name="totalPrice" value="₹<%= Veg.price %>"  readonly>
			
    		<button type="submit"  >Submit</button>
    		<!-- <button type="button" class="btn btn-outline-danger" onclick="cancel()">Cancel</button> -->
			
		</form>
	</div>
	<script type="text/javascript">
		document.addEventListener("DOMContentLoaded", function() {
			
			const quantityInput = document.getElementById('price');
			
			const basePriceInput = document.getElementById('basePrice');
		
			const totalPriceInput = document.getElementById('totalPriceInput');
		
			quantityInput.addEventListener('input', function() {
				let quantity = parseFloat(quantityInput.value);
				quantity = isNaN(quantity) ? 0 : quantity;
				let basePrice = parseFloat(basePriceInput.value);
				basePrice = isNaN(basePrice) ? 0 : basePrice;
		
				const totalPrice = quantity * basePrice;
				totalPriceInput.value = totalPrice.toFixed(2); // Displaying with 2 decimal places
			});
		});
		// Listen for the right-click event
// document.addEventListener('contextmenu', function(event) {
// 	setInterval(function() {
//     location.reload();
// }, 1000); 
    
//     // Prevent the default context menu from appearing
//     event.preventDefault();
// });
  // Store the original HTML content
 

// Check for changes every second
setInterval(checkForChanges, 1000);


	</script>
	<style>
		/*body{
	text-align: center;
}*/


.container{
	
	justify-content: center;
	align-items: center;
	
}
h1{
	margin-left: 15rem;
}
form{
	display: flex;
	flex-direction: column;
	margin-top: 3rem;
	margin-left: 15rem;
}
form input{
	margin-top: 0.5rem;
	width: 40rem;
	border: 0.5px solid gray;
	border-radius: 20px;
	height: 50px;
	outline: none;
}
form button{
	border-radius: 24px !important;
	width: 20rem;
	height: 40px;
	border: 2px solid red;
	background-color:white;
	font-size: 20px;
	color: black;

}
form button:hover{
 background-color: red;
 color: white;
}
form label{
	font-size: 18px;
	color: black;
}
@media(max-width:768px){
	h1{
		margin-left: 20px;
	}
	form{
		
		margin-left: 20px;
	}
	form input{
		
		width: auto;
		
	}
	form button{
		
		width: auto;
		
	
	}
	
}
	</style>
	<%-include("../includes/footer.ejs")%>